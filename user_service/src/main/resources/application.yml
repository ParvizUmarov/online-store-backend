spring:
  application:
    name: user.service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/user_db
    username: parviz
    password: qwerty

server:
  port: 8081