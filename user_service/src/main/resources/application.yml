spring:
  application:
    name: user.service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/user_db
    username: parviz
    password: qwerty

server:
  port: 8081